#!/bin/bash -e

# BROKER_IP=$(ctx source instance runtime_properties rabbitmq_endpoint_ip)

# ctx logger info "Setting Broker IP: "${BROKER_IP}" in provider_context under cloudify.cloudify_agent.broker_ip..."
# ctx target instance runtime_properties provider_context.cloudify.cloudify_agent.broker_ip "${BROKER_IP}"
# ctx logger info "Provider context is: $(ctx target instance runtime_properties provider_context)"
